const { Router } = require( 'express' );

const  router = Router();

//  http://localhost:3000/api/categories
router.get('/',);
router.post('/',);
router.patch('/:id',);
router.delete('/:id',);


module.exports = router;